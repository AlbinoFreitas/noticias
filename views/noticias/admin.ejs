<% include ../partials/header %>

      <div class="starter-template">
        <h1>Noticias Admin</h1>
        <table>
          <% noticias.forEach(notcia => { %>
            <tr>
              <td><%= notcia.title %></td>
            </tr>
          <% }) %>
        </table>
      </div>

<% include ../partials/footer %>